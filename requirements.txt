# SafeNet AI — Production Dependencies for Vercel Serverless
# Minimal footprint — only what the note checker and core API need.
# Heavy ML/graph packages are lazy-loaded inside their respective modules.

fastapi==0.115.0
pydantic==2.9.0
python-dotenv==1.0.1
numpy==1.26.4
pillow==10.0.0
google-genai
python-multipart==0.0.9
mangum==0.17.0

# Optional — these are only used by modules that gracefully fall back when absent
# networkx==3.3
# pandas==2.2.2
# scipy==1.11.4
