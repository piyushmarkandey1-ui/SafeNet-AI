# SafeNet AI — Vercel Serverless Python Dependencies
# This is the LIGHTWEIGHT version for Vercel (no torch/ML libs — exceeds 500 MB limit).
# Heavy ML modules (counterfeit vision) will gracefully fall back to mock on Vercel.
fastapi
uvicorn[standard]
pydantic>=2.0
python-dotenv
python-multipart
networkx
pandas
numpy
langchain
langgraph
fpdf2
google-generativeai
